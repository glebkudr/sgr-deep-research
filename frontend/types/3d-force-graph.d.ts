declare module '3d-force-graph' {
	// The default export is a function that returns a factory accepting an HTMLElement
	// and returning a graph instance with chainable API methods we use.
	// We type minimally as any for flexibility.
	const ForceGraph3D: any;
	export default ForceGraph3D;
}
